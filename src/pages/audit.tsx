import React from 'react'
import Layout from '@/components/Layout'
import Hero from '@/components/Hero'

const Audit: React.FC = () => {
  return (
    <Layout>
      <Hero title="Audit"/>
      <p className='text-center'>Audit Page here</p>
    </Layout>
  );
};

export default Audit;
